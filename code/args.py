#
'''
def create_args():# 字典，返回dict
    dic={}
    dic['num_fact']= #sequence_length=sentence.shape[1] 一次輸入的詞匯的個數
    dic['vocab_size']= #imput sentence_vocab max lenth +1
    dic['embedding_size']= 32 #full-connect dim
    dic['num_filters']= 64
    dic['filter_size']= [2,3,4,5] #(kernel_size)
    dic['lstm_hidden_layer_size']=
    dic['']=

    return dic

'''
num_fact = 
vocab_size = #imput sentence_vocab max lenth +1
embedding_size = 32 #full-connect dim
num_filters = 64
filter_size = [2,3,4,5] #(kernel_size)
lstm_hidden_layer_size =
